# 飞鹤快拼方案的配置补丁
# 覆盖 flypy_xhfast.schema.yaml 和 全局配置中的部分参数。
# 在此方案下，这里的配置具有最高优先级。

##  一些常用的设置项, 找到需要进行的设置所在的行,
##  取消注释 (删除行开头的单个 # 符号) 即可启用该设置项.
##  注意不要删除前面的空格, YAML 文件的缩进比较严格, 缩进不对容易报错.
---

patch:
  # 候选菜单
  menu/+:
    # 注释掉下行以恢复常规候选标签序号
    alternative_select_labels: [ ①, ②, ③, ④, ⑤, ⑥, ⑦, ⑧, ⑨, ⓪ ]
    alternative_select_keys: "1;.4567"      # 候选项选中快捷键
    # alternative_select_keys: 1234567890   # 候选项选中快捷键
    page_size: 7                            # 自定义候选项个数

  # 候选菜单布局样式
  style/+:
    font_point: 17                    # 候选字大小
    line_spacing: 5                   # 行间距大小
    show_paging: false                # 是否显示分页符号
    inline_preedit: false             # 编码位是否嵌入候选框
    text_orientation: horizontal      # 候选文字横向(horizontal)/纵向(vertical)
    candidate_list_layout: stacked    # 候选菜单横向(linear)/纵向(stacked)

  switches/+:
    - name: emoji
      states: [🈚️, 😄]
      reset: 1
    - name: prediction  # 仅用于移动端
      states: [休想, 联想]
    - name: char_mode
      states: [词组优先, 单字优先]
    - name: charset
      states: [通用字集, 增廣字集]
      reset: 1
    - name: charset_hint
      states: [码区隐藏, 码区显示]

  engine/+:
    processors/+:
    segmentors/+:
    translators/+:
    filters/+:
  'engine/processors/@next': predictor
  'engine/translators/@before 0': predict_translator
  # 注释下行, 以取消中英混合候选词自动添加的空格
  'engine/filters/@before last': lua_filter@*word_append_space*filter
  # 注释下行, 以取消中英混合输出上屏时自动添加的前导空格
  # 'engine/processors/@after 1': lua_processor@*word_append_space*processor

  key_binder/+:
    import_preset: default            # 从 default.yaml 继承通用的
    simpy_expand_key: "8"             # `8/Control+q` 简码成语优先
    char_mode: "Control+s"            # 单字模式优先
    pin_cand: "Control+t"             # 候选词置顶
    unpin_cand: "Control+t"           # 候选词取消置顶
    drop_cand: "Control+d"            # 强制删词, 无视输入的编码
    hide_cand: "Control+x"            # 匹配当前输入码后隐藏指定的候选字词
    reduce_fq_cand: "Control+j"       # 匹配当前输入码后降频指定的候选字词
    switch_comment: "Control+n"       # 切换注释(小鹤形码 / 带调拼音 / 无)
    commit_comment: "Control+p"       # 注释上屏
    switch_english: "Control+g"       # 切换到英文输入方案
    remove_user_word: "Control+r"     # 用户词典里的字词调频为负
    easydict_translate: "Control+y"   # 调用 Easydict 翻译高亮字词
    symbol_menu_fallback: "slash"     # 符号二级菜单返回主菜单键
    select_first_character: "9"       # `9/minus` 以词定字(首字)
    select_last_character: "0"        # `0/equal` 以词定字(尾字)
    bindings/+:
      # - {when: has_menu, accept: ";", send: 2}                # 引号; 用于第2候选
      # - {when: has_menu, accept: "'", send: 3}                # 引号' 用于第3候选
      - {when: paging,    accept: comma, send: Page_Up}         # 逗号, 返回上一页
      - {when: has_menu,  accept: ']', send: Page_Down}         # 括号] 下一页
      # - {when: paging,    accept: minus,  send: Page_Up}      # 减号- 返回上一页
      # - {when: paging,    accept: equal,  send: Page_Down}    # 等号= 下一页
      # - {when: has_menu,  accept: period, send: Page_Down}    # 句号. 下一页
      - {when: always, accept: Control+Shift+0, select: .next}
      - {when: always, accept: Control+Shift+g, select: easy_en}
      - {when: composing, accept: Tab,      send: Control+Right}
      - {when: composing, accept: Shift+Tab, send: Control+Left}
      - {when: composing, accept: Control+i, send: Control+Left}
      - {when: composing, accept: Control+o, send: Control+Right}
      - {when: composing, accept: Control+w, send: Control+BackSpace}
      # 以下按键绑定规则(含`tag` / `match`)需 flypy_keybinder 支持
      - {tag: "calculator", accept: "'", send_sequence: '{Page_Down}'}
      # - {match: "^cC.*$", accept: "'",  send_sequence: '{Page_Down}'}

  recognizer/+:
    patterns/+:
      timestamp: "^/uts$"                       # 时间戳
      week: "^(/wk|week)$"                      # 星期
      time: "^(/wt|time)$"                      # 时间
      date: "^(/wd|date)$"                      # 日期
      history: "^(/hs|hH)$"                     # 上屏历史
      LaTeX: "^(/lt|lT)(.*)$"                   # LaTeX排版
      flypy_help: "^(/oh|oH)$"                  # 帮助菜单
      switch_option: "^(/so|sO)$"               # 切换配置选项
      shortcuts_app: "^/jk[a-z]*$"              # 快捷指令(启动应用)
      shortcuts_cmd: "^/kj[a-z]*$"              # 快捷指令(执行命令)
      flypy_key_map: "^/ok[a-z]*$"              # 小鹤双拼键位帮助
      calculator: "^(/=|cC)([^,;']*)$"          # 计算器(可查帮助)
      chinese_lunar:  "^(/nl|nL)[0-9]*$"        # 中国农历(可反查)
      chinese_number: "^(/nn|nN)[0-9.-]+$"      # 中文大写数字(金额)
      radical_lookup: "^(/rl|rL)[a-z]+'?$"      # 单字部件拆字(查拼音)

  punctuator/+:
    digit_separators: ".:"            # 指定要转换为英文半角的符号, 若双击则恢复全角(中文)
    # digit_separator_action: commit
    symbol_menu_prefix: '/vs'
    symbol_normal_prefix: '/'
    symbols/+:
    full_shape/+:
    half_shape/+:
      # '"': {pair: ['‘', '’']}
      # "'": {pair: ['“', '”']}
      # ";": {commit: "；"}                     # 取消注释后, 恢复默认分号
      ";": [";", "；"]                          # 添加注释, 以恢复默认分号
      "_": "_"
      '\': ['､', '、', '\', '＼', '\r', '\n', '\t']
      "@":
        - "@"
        # - "@xxx.com"
        - "@gmail.com"
        - "@foxmail.com"
        - "@outlook.com"
        - "@hotmail.com"
        - "@qq.com"
        - "@163.com"
        - "@139.com"

  speller/+:
    auto_select_char: true            # 单字全码唯一时则自动顶屏(包括直接辅码)
    auto_select_phrase: true          # 单字或词组加间接辅码唯一时则自动上屏
    enable_fuzz_algebra: true         # 是否开启简拼功能(候选字词含有中英数混杂项)
    algebra/+:
      # - derive|/||                  # 取消注释,直接辅码(新手不推荐)

      ## 模糊音:
      - derive/^([qx])v/$1u/
      - derive/^([jy])u/$1v/
      - derive/^([ny])b/$1k/          # in => ing
      - derive/^(i)g/$1f/             # eng => en
      - derive/^z([ry])/v$1/          # z => zh
      - derive/^c([az])/i$1/          # c => ch
      - derive/^u([hk])/s$1/          # sh => s
      # - derive/^l(.*)/n$1/          # l => n
      # - derive/^n(.*)/l$1/          # n => l
      # - derive/^v(.*)/z$1/          # zh => z
      # - derive/^i(.*)/c$1/          # ch => c
      # - derive/^u(.*)/s$1/          # sh => s
      # - derive/^(.*)k/$1b/          # ing => in
      # - derive/^(.*)g/$1f/          # [zcs][h]eng => [zcs][h]en
      # - derive/^(.*)f/$1g/          # [zcs][h]en => [zcs][h]eng

  easy_en/+:                          # 英文单词输入方案
    tips: "英文"
    wildcard: '*'
    expand_word_count: 666            # 模糊匹配最多迭代数

  custom_phrase/+:                    # 自定义短语
    comment_mark: " 📌"               # 注解标记

  flypy_key_map/+:                   # 小鹤双拼键位帮助
    prefix: "/ok"
    tips: "双拼查键"

  radical_reverse_lookup/+:           # 部件组字反查拼音
    overwrite_comment: false
    dictionary: flyhe_fast            # 可自行更换反查词典

  pin_word:                           # 词条置顶配置
    word_quality: 1000
    comment_mark: " ᵀᴼᴾ"              # 置顶词标记
    # comment_mark: " 🔝"             # 置顶词标记

  pair_punct:                         # 符号配对开关
    enable: true

  emoji_reduce:                       # Emoji 降频
    index: 6                          # 到 index 位置

  cold_word_reduce:                   # 高亮词条降频
    index: 4                          # 到 index 位置
    excluded_types: ["easy_en"]

  LaTeX:                              # LaTeX 公式输入
    tag: LaTeX
    # prefix: "lT"
    prefix: "/lt"
    tips: "LaTeX 公式"

  history:                            # 上屏历史记录
    tag: history
    # prefix: "/hs"
    prefix: "hH"
    tips: "上屏历史"
    max_count: 99
    initial_quality: 999
    comment_max_length: 9
    excluded_types: ["punct"]

  calculator:                         # 简易计算器
    tag: calculator
    # prefix: "/="
    prefix: "cC"
    suffix: "'"
    tips: "计算器"

  chinese_number:                     # 中文大写数字(金额)
    # prefix: "/nn"
    prefix: "nN"
    suffix: "'"
    tips: "中文数字"
    tag: chinese_number
    select_keys: "sdfjklm"

  chinese_lunar:                      # 中国农历(可反查)
    tag: chinese_lunar
    # prefix: "/nl"
    prefix: "nL"
    suffix: "'"
    tips: "农历"

  predictor:                          # 候选字词联想预测
    db: predict.db
    max_candidates: 7                 # 联想的候选项个数
    max_iterations: 1                 # 联想预测的次数
